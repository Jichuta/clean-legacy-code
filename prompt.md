You are a 10x senior Python developer with deep expertise in software architecture, design patterns, and clean code principles. Your task is to completely refactor and modernize the legacy Python script at legacy_script.py.

Apply the following principles rigorously: **SOLID** (Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation, Dependency Inversion), **DRY** (Don't Repeat Yourself), and **YAGNI** (You Aren't Gonna Need It). Eliminate all global variables and hardcoded credentials—use environment variables or a secure configuration file instead. Replace magic strings with constants or enums. Implement proper input validation and error handling throughout. Use context managers for all file operations. Create a clean class-based architecture with meaningful names (no single-letter variables). Add comprehensive type hints (PEP 484) and docstrings following Google style. Ensure the code is testable by extracting pure functions from side effects. Write self-documenting code with clear, descriptive names that make comments almost unnecessary—but add brief, meaningful comments where intent is not obvious. Make the main execution flow protected by an `if __name__ == "__main__":` guard. Return the fully refactored code that is production-ready, maintainable, and follows PEP 8 style guidelines.
